local stb_dest_dir = mg.get_build_dir() .. 'deps/stb/'
-- local stb_url = 'https://github.com/nothings/stb/archive/refs/heads/master.zip'
-- if net.download(stb_url, stb_dest_dir) then
-- 	io.write('stb: Updated \'master\' branch\n')
-- else
-- 	io.write('stb: up-to-date\n')
-- end

-- return {includes = {'deps/' .. stb_dest_dir}}